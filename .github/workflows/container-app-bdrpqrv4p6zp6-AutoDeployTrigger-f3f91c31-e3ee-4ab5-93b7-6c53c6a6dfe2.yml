name: Trigger auto deployment for container-app-bdrpqrv4p6zp6

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'result/**'
    - '.github/workflows/container-app-bdrpqrv4p6zp6-AutoDeployTrigger-f3f91c31-e3ee-4ab5-93b7-6c53c6a6dfe2.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPBDRPQRV4P6ZP6_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPBDRPQRV4P6ZP6_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPBDRPQRV4P6ZP6_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v1
        with:
          appSourcePath: ${{ github.workspace }}/result 
          registryUrl: containerregistrybdrpqrv4p6zp6.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPBDRPQRV4P6ZP6_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPBDRPQRV4P6ZP6_REGISTRY_PASSWORD }}
          containerAppName: container-app-bdrpqrv4p6zp6
          resourceGroup: testsecurestringexmaplevoting
          imageToBuild: containerregistrybdrpqrv4p6zp6.azurecr.io/container-app-bdrpqrv4p6zp6:${{ github.sha }}
          dockerfilePath: Dockerfile


