name: Trigger auto deployment for container-app-l55l4ltawx6mc

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'result/**'
    - '.github/workflows/container-app-l55l4ltawx6mc-AutoDeployTrigger-88d5e54a-1866-4aba-aeff-f6f2bb191886.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPL55L4LTAWX6MC_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPL55L4LTAWX6MC_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPL55L4LTAWX6MC_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v1
        with:
          appSourcePath: ${{ github.workspace }}/result 
          registryUrl: containerregistryl55l4ltawx6mc.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPL55L4LTAWX6MC_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPL55L4LTAWX6MC_REGISTRY_PASSWORD }}
          containerAppName: container-app-l55l4ltawx6mc
          resourceGroup: examplevotingappWQithNewApiVersion
          imageToBuild: containerregistryl55l4ltawx6mc.azurecr.io/container-app-l55l4ltawx6mc:${{ github.sha }}
          dockerfilePath: Dockerfile


