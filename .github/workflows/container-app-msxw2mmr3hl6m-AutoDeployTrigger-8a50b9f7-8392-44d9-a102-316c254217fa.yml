name: Trigger auto deployment for container-app-msxw2mmr3hl6m

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'result/**'
    - '.github/workflows/container-app-msxw2mmr3hl6m-AutoDeployTrigger-8a50b9f7-8392-44d9-a102-316c254217fa.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPMSXW2MMR3HL6M_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPMSXW2MMR3HL6M_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPMSXW2MMR3HL6M_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v1
        with:
          appSourcePath: ${{ github.workspace }}/result 
          registryUrl: containerregistrymsxw2mmr3hl6m.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPMSXW2MMR3HL6M_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPMSXW2MMR3HL6M_REGISTRY_PASSWORD }}
          containerAppName: container-app-msxw2mmr3hl6m
          resourceGroup: examplevotingapp8346
          imageToBuild: containerregistrymsxw2mmr3hl6m.azurecr.io/container-app-msxw2mmr3hl6m:${{ github.sha }}
          dockerfilePath: Dockerfile


