name: Trigger auto deployment for container-app-hr4hdvmh3lzqo

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'vote/**'
    - '.github/workflows/container-app-hr4hdvmh3lzqo-AutoDeployTrigger-3cf7cea3-e28e-4d04-8c08-f7bdadeb3bc3.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPHR4HDVMH3LZQO_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPHR4HDVMH3LZQO_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPHR4HDVMH3LZQO_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v1
        with:
          appSourcePath: ${{ github.workspace }}/vote 
          registryUrl: containerregistryhr4hdvmh3lzqo.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPHR4HDVMH3LZQO_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPHR4HDVMH3LZQO_REGISTRY_PASSWORD }}
          containerAppName: container-app-hr4hdvmh3lzqo
          resourceGroup: examplevotingapp970c
          imageToBuild: containerregistryhr4hdvmh3lzqo.azurecr.io/container-app-hr4hdvmh3lzqo:${{ github.sha }}
          dockerfilePath: Dockerfile


