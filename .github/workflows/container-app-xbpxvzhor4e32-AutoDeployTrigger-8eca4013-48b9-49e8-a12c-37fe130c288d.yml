name: Trigger auto deployment for container-app-xbpxvzhor4e32

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'result/**'
    - '.github/workflows/container-app-xbpxvzhor4e32-AutoDeployTrigger-8eca4013-48b9-49e8-a12c-37fe130c288d.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPXBPXVZHOR4E32_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPXBPXVZHOR4E32_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPXBPXVZHOR4E32_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v1
        with:
          appSourcePath: ${{ github.workspace }}/result 
          registryUrl: containerregistryxbpxvzhor4e32.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPXBPXVZHOR4E32_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPXBPXVZHOR4E32_REGISTRY_PASSWORD }}
          containerAppName: container-app-xbpxvzhor4e32
          resourceGroup: examplevotingappaa0f
          imageToBuild: containerregistryxbpxvzhor4e32.azurecr.io/container-app-xbpxvzhor4e32:${{ github.sha }}
          dockerfilePath: Dockerfile


